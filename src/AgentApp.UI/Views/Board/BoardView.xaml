<UserControl x:Class="AgentApp.UI.Views.Board.BoardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- US-091/US-107: Project board filter -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Project ID:" VerticalAlignment="Center" Margin="0,0,6,0" Foreground="#888" />
            <TextBox Text="{Binding CurrentProjectId, UpdateSourceTrigger=PropertyChanged}"
                     Width="260" MinHeight="30" Margin="0,0,6,0" />
            <Button Content="Load Board" Command="{Binding LoadBoardCommand}" MinWidth="90" MinHeight="30" />
        </StackPanel>

        <!-- US-112: Review Agent trigger -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="Trigger Review Agent" Command="{Binding TriggerReviewAgentCommand}"
                    MinWidth="150" MinHeight="30" IsEnabled="{Binding IsReviewRunning,
                        Converter={StaticResource BooleanInverseConverter}}" />
        </StackPanel>

        <!-- US-091: Board records list -->
        <ListView Grid.Row="2" ItemsSource="{Binding Records}" Margin="0,0,0,8">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Title" Width="300" DisplayMemberBinding="{Binding Title}" />
                    <GridViewColumn Header="Status" Width="160" DisplayMemberBinding="{Binding Status}" />
                    <GridViewColumn Header="Type" Width="120" DisplayMemberBinding="{Binding RecordType}" />
                </GridView>
            </ListView.View>
        </ListView>

        <!-- US-113: Scheduler status header -->
        <TextBlock Grid.Row="3" Text="Scheduler Status" FontWeight="SemiBold"
                   Margin="0,0,0,4" Foreground="#A0C4FF" />

        <!-- US-113: Scheduler status list -->
        <ListView Grid.Row="4" ItemsSource="{Binding SchedulerStatuses}" Margin="0,0,0,8"
                  MaxHeight="140">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Job" Width="160" DisplayMemberBinding="{Binding JobType}" />
                    <GridViewColumn Header="Enabled" Width="80" DisplayMemberBinding="{Binding IsEnabled}" />
                    <GridViewColumn Header="Last Run" Width="140" DisplayMemberBinding="{Binding LastRun}" />
                    <GridViewColumn Header="Next Run" Width="140" DisplayMemberBinding="{Binding NextRun}" />
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Status -->
        <TextBlock Grid.Row="5" Text="{Binding StatusMessage}"
                   Foreground="#A0C4FF" FontSize="12" Opacity="0.8" />
    </Grid>
</UserControl>
