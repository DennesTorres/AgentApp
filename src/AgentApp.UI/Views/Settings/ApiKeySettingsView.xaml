<UserControl x:Class="AgentApp.UI.Views.Settings.ApiKeySettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="600">
    <StackPanel Margin="20" MaxWidth="500" HorizontalAlignment="Left">
        <TextBlock Text="Model API Key"
                   FontSize="16" FontWeight="SemiBold"
                   Margin="0,0,0,16" />

        <TextBlock Text="Azure AI Foundry endpoint: https://msdnfoundry.services.ai.azure.com"
                   FontSize="11" Opacity="0.6" Margin="0,0,0,12" />

        <TextBlock Text="API Key" FontSize="12" Margin="0,0,0,4" />
        <PasswordBox x:Name="ApiKeyBox"
                     PasswordChanged="ApiKeyBox_PasswordChanged"
                     MinHeight="32"
                     Margin="0,0,0,12" />

        <Button Content="Save Key"
                Command="{Binding SaveKeyCommand}"
                MinWidth="100" MinHeight="32"
                HorizontalAlignment="Left"
                Margin="0,0,0,12" />

        <TextBlock Text="{Binding StatusMessage}"
                   FontSize="11" Opacity="0.7"
                   TextWrapping="Wrap" />
    </StackPanel>
</UserControl>
