<UserControl x:Class="AgentApp.UI.Views.LearningProposalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Border Background="#1A2A1A" BorderBrush="#2E7D32" BorderThickness="0,2,0,0"
            Padding="12,8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- Header -->
            <TextBlock Grid.Row="0" Grid.ColumnSpan="3"
                       Text="Learning Proposal — Proposed Rule Change"
                       FontWeight="SemiBold" FontSize="12"
                       Foreground="#A5D6A7" Margin="0,0,0,6" />

            <!-- Summary -->
            <TextBlock Grid.Row="1" Grid.Column="0"
                       Text="{Binding HumanSummary}"
                       TextWrapping="Wrap" FontSize="11"
                       Margin="0,0,8,6" />

            <!-- File -->
            <TextBlock Grid.Row="2" Grid.Column="0"
                       FontSize="10" Opacity="0.6"
                       Margin="0,0,0,0">
                <Run Text="File: " /><Run Text="{Binding FileName}" />
            </TextBlock>

            <!-- Buttons -->
            <Button Grid.Row="1" Grid.RowSpan="2" Grid.Column="1"
                    Content="Approve"
                    Command="{Binding ApproveCommand}"
                    MinWidth="80" Margin="4,0,4,0" />
            <Button Grid.Row="1" Grid.RowSpan="2" Grid.Column="2"
                    Content="Reject"
                    Command="{Binding RejectCommand}"
                    MinWidth="80" Margin="0,0,0,0" />
        </Grid>
    </Border>
</UserControl>
