<UserControl x:Class="AgentApp.UI.Views.Projects.ProjectListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Projects" FontSize="18" FontWeight="SemiBold"
                   Margin="0,0,0,12" />

        <ListBox Grid.Row="1"
                 ItemsSource="{Binding Projects}"
                 BorderThickness="0">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="8,6" />
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <!-- Row: name + date + Open + gear -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="14" />
                                <TextBlock Text="{Binding LastAccessed}" FontSize="11" Opacity="0.5" />
                            </StackPanel>
                            <!-- C-086: Open — navigate to most recent session or create one -->
                            <Button Grid.Column="1"
                                    Content="Open"
                                    Command="{Binding DataContext.OpenProjectCommand,
                                        RelativeSource={RelativeSource AncestorType=ListBox}}"
                                    CommandParameter="{Binding}"
                                    MinWidth="60" MinHeight="28" Margin="8,0,4,0" />
                            <!-- C-087: gear toggle — show/hide configuration details -->
                            <Button Grid.Column="2"
                                    Content="⚙"
                                    Command="{Binding ToggleExpandCommand}"
                                    MinWidth="28" MinHeight="28" />
                        </Grid>

                        <!-- C-087: expandable configuration panel -->
                        <Border Margin="0,6,0,0" Padding="10,8"
                                Background="#1A1A2E" CornerRadius="4"
                                Visibility="{Binding IsExpanded,
                                    Converter={StaticResource BooleanToVisibilityConverter}}">
                            <StackPanel>
                                <Grid Margin="0,0,0,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="120" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="Intent:" Opacity="0.6" FontSize="12" />
                                    <TextBlock Grid.Column="1" Text="{Binding Purpose}" FontSize="12" TextWrapping="Wrap" />
                                </Grid>
                                <Grid Margin="0,0,0,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="120" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="Agent folder:" Opacity="0.6" FontSize="12" />
                                    <TextBlock Grid.Column="1" Text="{Binding AgentFolder}" FontSize="12"
                                               TextWrapping="Wrap" FontFamily="Consolas" />
                                </Grid>
                                <Grid Margin="0,0,0,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="120" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="Code folder:" Opacity="0.6" FontSize="12" />
                                    <TextBlock Grid.Column="1" Text="{Binding CodeFolder}" FontSize="12"
                                               TextWrapping="Wrap" FontFamily="Consolas" />
                                </Grid>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="120" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="Allowed paths:" Opacity="0.6" FontSize="12" />
                                    <ItemsControl Grid.Column="1" ItemsSource="{Binding AlwaysAllowedPaths}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding}" FontSize="12" FontFamily="Consolas" />
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </Grid>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
